export type Candidat = {
  _id?: string;
  email: string;
  phone: string;
  published?: boolean;
  address: string;
  description: string;
  name: string;
  city: string;
  postalCode: string;
  jobTitle: string;
  province: string;
  updatedAt?: string;
  createdAt?: string;
};
